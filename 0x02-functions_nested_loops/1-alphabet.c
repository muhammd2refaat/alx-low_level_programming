#include "main.h"

/**
 * print_alphabet - print alphabet in lowercase
 */

void print_alphabet(void)
{
        char leter;

        for (leter = 'a'; leter <= 'z'; leter++)
                _putchar(leter);

        _putchar('\n');
}
